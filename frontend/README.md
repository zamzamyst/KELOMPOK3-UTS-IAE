# Frontend Dashboard

This file contains a small dashboard that links to the four microservices. It is intended to run on port `8000`.

Run:

```bash
cd c:/Laragon/www/KELOMPOK3-UTS-IAE/frontend
php -S 127.0.0.1:8000 -t .
```

Edit `config.php` to change service base URLs.
